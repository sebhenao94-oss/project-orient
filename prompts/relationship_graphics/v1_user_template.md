Extract relationship evidence from this BMS graphic page screenshot.

Screenshot filename (context only — the on-page title is authoritative): {source_filename}

Return the strict evidence JSON described in your instructions. JSON only.
